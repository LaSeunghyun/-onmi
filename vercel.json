{
  "version": 2,
  "routes": [
    {
      "src": "/api/cron/crawl",
      "dest": "api/cron/crawl.py"
    },
    {
      "src": "/api/cron/daily-report",
      "dest": "api/cron/daily-report.py"
    },
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ],
  "crons": [
    {
      "_comment_kst": "한국 시간(KST, UTC+9) 기준 매 시간 정각 실행. 크론은 UTC 기준으로 실행되지만, 코드 내부에서 자동으로 한국 시간으로 변환하여 사용자 조회 및 요약 생성이 수행됩니다.",
      "path": "/api/cron/daily-report",
      "schedule": "0 * * * *"
    }
  ],
  "env": {
    "PYTHONPATH": ".:backend/shared:backend/api-gateway/src"
  },
  "functions": {
    "api/index.py": {
      "maxDuration": 30,
      "includeFiles": "{backend/shared/**,backend/api-gateway/src/**,config/**}",
      "excludeFiles": "{backend/**/venv/**,backend/**/__pycache__/**}"
    },
    "api/cron/crawl.py": {
      "maxDuration": 300,
      "includeFiles": "{backend/shared/**,backend/api-gateway/src/**,config/**}",
      "excludeFiles": "{backend/**/venv/**,backend/**/__pycache__/**}"
    },
    "api/cron/daily-report.py": {
      "maxDuration": 300,
      "includeFiles": "{backend/shared/**,backend/api-gateway/src/**,config/**}",
      "excludeFiles": "{backend/**/venv/**,backend/**/__pycache__/**}"
    }
  },
  "regions": ["icn1"]
}

